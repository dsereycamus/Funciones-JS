// Transforma esta declaración de función a una función de expresión:
function example(a, b, c){
    return a+b+c
    }

// Se transforma a función de expresión: 

let funcionExpresion =  function(a, b, c){
    return a + b + c;
}
